package ru.blinov.mygeekspringboot;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
//File –> Setting –> Build, Execution, Deployment –> Compiler –> check this Build project automatically
@SpringBootApplication
public class MyGeekSpringBootApplication {

    public static void main(String[] args) {
        SpringApplication.run(MyGeekSpringBootApplication.class, args);
    }
}

//File –> Setting –> Build, Execution, Deployment –> Compiler –> check this Build project automatically